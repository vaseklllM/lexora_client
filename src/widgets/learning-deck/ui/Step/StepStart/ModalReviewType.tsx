import { ModalRepeatCardsType } from "@/entities/modal-repeat-cards-type";
import { ReactElement } from "react";

export const ModalReviewType = (): ReactElement => {
  return <ModalRepeatCardsType isOpen />;
};
