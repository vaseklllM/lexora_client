// import { create } from "zustand";

// type State = {
//   part: number;
//   finishedCardIds: string[];
// };

// type Actions = {
//   finishCard: (cardId: string) => void;
//   nextPart: () => void;
// };

// type Store = State & Actions;

// export const useSliceCardsStore = create<Store>((set) => ({}));
